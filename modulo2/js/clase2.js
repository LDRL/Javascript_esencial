var first_name = 'Sacha', last_name = 'Lifszyc';

var nameInMayuscula = first_name.toUpperCase();
var nameInMinuscula = last_name.toLowerCase();

var firstLetterName = first_name.charAt(0)

var countLetterName = 5
first_name = 'Federico'
countLetterName = first_name.length

var fullName = first_name + ' ' + last_name

// inteporasion de texto ``

var fullNameInteporasion = `${first_name} ${last_name}`

var str = first_name.substr(1,2)
console.log(str)
var late_name = first_name.charAt(first_name.length -1)
console.log(late_name)